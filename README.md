# 火影忍者手游替身计时器

Windows 10/11 x64 · MuMu · 本地视觉识别 · MIT

识别游戏画面的替身豆，在悬浮窗显示对方替身倒计时。模型、忍者资料、场景模板和 OCR 运行依赖均已内置。

## 下载与使用

到 [最新发布页](https://github.com/fangxiaobaiqaq1/naruto-substitute-timer/releases/latest) 选择：

- **Setup 安装包**：推荐首次使用。创建快捷方式和卸载入口，不需要管理员权限。
- **timer-app.exe**：便携版，放在可写目录直接运行。
- **完整 ZIP**：附带诊断工具和第三方许可。

启动 MuMu 与火影忍者，然后打开计时器：

1. 在「设置 → 常规」选择我方在左或右；也可填写账号名使用自动认边。
2. 默认自动寻找运行游戏的 MuMu 实例。多开或需要指定时，在「设置 → 模拟器」在可见进程列表中点击窗口名称（显示PID和实例编号）。
3. 进入对局后自动识别豆数并计时。训练场也可使用；看不清时显示未知，不凭旧画面猜测替身。

需要已安装且正在运行的受支持 MuMu；不需要 Go、Python 或额外安装 OCR/VC 运行库。软件未签名，Windows 仍可能显示下载信誉提示。

## 更新与版本

悬浮窗标题与「关于与更新」显示当前版本。正式版默认在启动约 10 秒后检查 GitHub Releases，之后最多每 6 小时检查一次；发现新版时浮窗出现入口。自动检查只提醒，**绝不自动下载、安装或重启**，可在「设置 → 常规 → 外观与更新」关闭。点击「检查更新」可立即刷新并查看发布日期和更新日志。

在线更新流程：下载 EXE → 验证 GitHub 提供的 SHA256、文件大小和版本 → 点击「重启并更新」。断网、下载中断或校验失败不会覆盖当前程序。更新保留配置，并保存一个旧 EXE 备份。v0.1.x / v0.2.0 用户先手动检查或下载一次 v0.2.1；之后新版可自动提醒。

## 设置、数据与诊断

- 安装版配置和诊断录制：`%APPDATA%\NarutoTimer`；便携版：程序目录中的 `config.json` 和 `debug`。
- OCR 与更新缓存：`%LOCALAPPDATA%\naruto-timer`。卸载程序保留个人设置和日志。
- 「设置 → 常规 → 外观与更新」可调整**计时浮窗**不透明度（40%–100%）和字号（80%–160%）；拖动立即预览，松开后保存。设置、更新和错误窗口始终不透明，避免影响操作。
- 「诊断」可查看采集来源、OCR 状态、延迟报告和原帧录制。录制需手动开启，只有有效对局帧才保存，默认容量上限 1 GiB。
- 采集函数耗时不等于端到端延迟；没有有效帧或真实绘制回调时不报告完整端到端数据。

详见 [使用说明](docs/使用说明.md)、[诊断说明](docs/诊断说明.md)。

## 开发

Go 1.26.1、Windows x64 C/C++ 工具链（cgo）：

```powershell
$env:TIMER_VERSION = "v0.2.1"
.\build.bat
```

正式构建通过脚本接入原生绘制诊断，输出到 `bin/`。安装包构建及测试见 [开发与发布](docs/开发与发布.md)。部分历史图像回归需要本地 `inbox/` 证据，不随仓库分发；便携资源、更新协议、配置和合成界面测试可独立运行。

代码入口：`cmd/timer-app`、`internal/ui`、`internal/frame`、`internal/engine`、`internal/updates`。

## 许可

原创代码为 [MIT](LICENSE)。游戏素材、OCR 模型和微软运行库保留各自权利，见 [第三方声明](THIRD_PARTY_NOTICES.md)。程序仅处理模拟器画面，不读取游戏内存，不注入或修改游戏。
