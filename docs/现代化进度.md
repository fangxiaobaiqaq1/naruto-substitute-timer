# 现代化开发进度

原始完整目标（未缩减）：最终目标是最现代化的。 审查代码查号隐患 延迟最低。 然后什么都能检测出来 你完全可以调用mcp获得你想要的比如mumu的mcp

以下是阶段性工程记录，不表示总目标已完成，也不以已有截图测试替代全部识别覆盖。

## 已完成并验证（2026-09-06 本轮）

- 预编译 NCC 模板的统计量与 mask 连续区域，保留原算法的参与像素、扫描位置、分数及阈值。随机对照覆盖非零坐标、带 stride 子图、短 mask、稀疏/空 mask、常量图及并发读取。
- 场景识别只转换实际 ROI 的灰度，同区域模板变体共享数据。缓存只存当前几何的不可变模板，不存无限增长的截图。逐模板分数与整幅灰度路径完全一致。
- 补齐实机漏识别的决斗场入口：以「忍术对战」「段位赛」独立文字控件判为 lobby。包含 640～2560 宽缩放、背景变化、去除控件和战斗负例。
- 修正均值亮度即判空的逻辑：极暗/极亮背景上仍可见的 HUD 继续参与识别；近乎均匀的黑白画面仍判空。
- 配置保存改为临时完整快照、同步、同目录替换；本进程读写序列化，失败不截断旧配置。并发读写与失败清理有回归。

## 实测范围

证据目录：`debug/modernize-20260906-141329`。同一运行中的 MuMu 决斗场入口，各采 12 秒，不保存逐帧 PNG 以免编码干扰。
链路 P95 从 26.93ms 降至 17.50ms；观测速率从 37.1 次/秒升至 57.9 次/秒。最终 695 次均无采集错误，全部识别为 lobby。
这些数值包括截图 API、分析与交付，不包括源帧渲染、事件确认和显示器呈现，不能称为完整端到端延迟，也不能外推为全部对局的准确率。

## 下一阶段仍需推进

- [ ] 建立有真实标签的多场景/多技能/多忍者连续录屏集，分开统计漏检、误检、认边及事件确认延迟。
- [ ] 扩展对面忍者和 10s/15s CD 的自动识别，保留人工覆盖及不确定状态；不把猜测写成确定结果。
- [ ] 扩展没有现成姓名模板的账号识别，以及不同皮肤、特效与特殊豆槽布局。
- [ ] 测量完整的采集、确认、UI 排队和屏幕呈现延迟，而不仅是函数耗时。
- [ ] 继续检查缺失模板时的回退、SDK 重连、配置失效、关闭过程及资源释放的边界。
- [ ] 统一现代前端的状态反馈、识别可信度和诊断入口。

## 观察边界

当前链路使用 MuMu 的截图 SDK，分析已渲染像素。本轮未读取游戏进程内存、注入或改动游戏 APK，也未自动操作游戏对局；这不等于承诺不存在平台规则或账号风险。现有 MuMu MCP 的 ADB 路径无效，已用项目可用截图链路取得数据，没有因此修改全局 MCP 配置。

## 用户补充的专项规则（2026-09-07）

用户澄清特殊冷却对象是 **照美冥[五代目水影]**，不是所有照美冥；该版本替身时显示 15/10 秒同起点候选倒计时。其他忍者按 15 秒。该规则优先于旧解包表的 10/15s 数据推导，不再尝试从历史 ID 泛化特殊冷却。

本轮接入九张特殊忍者原始裁图：柱间/斑六槽、带土紫色四槽、暴怒第六尾鸣人红色四槽。名字决定可接受的槽数及配色，不能只看末端亮光就把普通四槽扩成六槽。末张鸣人图的人工可见标签为两颗亮豆、两颗空豆，不是零颗。

已经实现版本精确限定、同事件双时间估计、左右独立4/6槽及变更时的防误开钟。姓名快查逐帧验证上次的文字位置，文字消失即作废；未知名字最多每500ms全搜索一次，避免每帧反复扫描所有模板。

仍需：五代目水影姓名截图的自动识别校验；右侧真实特殊HUD与连续实机录像覆盖；无模板通用文字识别；完整呈现延迟。以上均未据合成测试宣告完成。

专项验证记录：`debug/special-ninja-20260906-215214/verification.json`。当前全包测试、go vet 与新代码相关的race检查均通过。完整RGB高分辨率矩阵的race运行曾因3分钟时限退出（正常非race矩阵通过）；没有把这个超时描述成整个项目race全部通过。新的主程序和配套工具已放入bin，旧版备份在对应before目录。

## 后台通用文字识别与未知名字搜索（2026-09-07）

- 系统 Windows 中文 OCR 已接入独立的单工作者：通过匿名管道传图，不写临时截图，不逐帧启动进程。主采集链路不等待识别；有单次超时、失败退避、截止/代次/几何/当前字形核对与关闭清理。
- 两个视图和两次采集结果一致才接受名字，账号完整匹配，模板/手动设置优先。不由文本放宽配色或4/6槽。五代目水影仍要求完整版本，仍没有该版本自然截图的准确率证据。
- 无姓名模板目录中的真实历史图，可识别山中井野与白方小并正确给出我方右侧；另有低清实图被拒绝或只能得到基础名字。OCR共识不是正确率保证，未配置的对手账号不提升为已知身份。
- 发现未知特殊名字的原分辨率搜索会超过500ms重试周期。现在480宽等效尺寸定位，再回到原像素按原阈值核验。直接同模板基准：单侧旧全扫描约303～310ms，新搜索约2.84～2.87ms（仅该录图/函数，不是画面到浮窗延迟）。九张特殊原图及全链路合成回归保持通过。
- 无模板实时识别录图测试约0.9秒建立文字共识，主 Analyze 最大约19～27ms（含冷态初始化的这组样本），常态记录多为5～9ms；不是实机全链路/呈现延迟。
- 本轮 MuMu SDK 实机诊断返回RPC1722，没有有效画面。原诊断误报0ms已修复：无帧时延迟null、有效帧率0、进程失败；保留尝试次数供排障。不据此宣称实机性能达标。

证据目录：`debug/async-hud-text-20260906-235143`。完整目标继续保留；尚需更多不同账号/忍者/特效的自然连续实机素材、五代目水影与右侧特殊HUD的实图、系统性准确率标签以及真正的采集到屏幕呈现延迟测量。

## 用户关闭 MuMu 后的离线自审（2026-09-07）

MuMu 无画面的原因已由用户确认是主动关闭。本轮未启动、操作模拟器或重新尝试在线采集；使用本机源码、模拟后端和已有截图审查。

- 查到上一轮 `all-tests-final.txt` 在用例输出通过后仍报 OCR 测试程序删除被拒绝，不能算完整成功。失败请求原来先注销进程再异步回收，使 Close 看不到尚未退出的助手。现改为等待 stdout 所有者完成 Wait，再注销；清理超时保留登记并返回错误。异常协议/超长行先取消子进程，避免等到整个请求超时；写管道取消回调也不得遗留到下一次复用。
- 增加确定性失败回归：进程回收前不得注销、超长协议及时失败、迟到回收后新进程需重新握手；原有超时重试、Close 取消阻塞读/写测试继续保留。
- 修复场景切换清空5秒失败退避的问题。后端健康与画面代次分离；旧画面的文字仍被拒绝，但后端失败不会因为进入大厅/重新对局而失去限频。
- 窗口关闭事件直接停止循环；迟到采集不能更新账号边、豆数、状态，已排队的界面回调在关闭后跳过。停止操作可重复调用。
- 修复文字消失后仍显示「已核对」及重试时旧错误遮盖当前状态；补了会在旧代码失败的测试。
- 视觉检查发现软件测试窗口未模拟 Fyne 原生窗口的最小尺寸约束，旧120px双计时预览挤压了按钮；核对本机 Fyne GLFW 的 Resize/fitContent 后修正测试预览夹具，并新增状态行与按钮不重叠检查。这是预览夹具问题，不据此宣称已复现原生窗口重叠。

证据目录：`debug/self-review-20260907-105426`。真实 Windows OCR 在无姓名模板目录中约827ms确认「山中井野（白方小）」及我方右侧，该录图主 Analyze 最大约19.07ms。特定未知名字搜索基准旧全扫描约294～303ms、新搜索约2.74～2.82ms。均非实机呈现延迟，也不代表全覆盖或全局最低延迟。五代目水影自然截图与真实连续场景覆盖仍未补齐，完整目标未完成。

交付校验：`go test ./... -count=1`、`go vet ./...`、OCR/HUD文字/UI/忍者包的race与最后的UI夹具race均返回0，真实Windows OCR离线测试通过。五个可执行程序已备份后更新到bin，安装后OCR诊断再次通过、助手退出后无残留；SHA256和各项退出码记录在该目录的 `installation.json` / `verification.json`。用户config.json散列保持不变。没有启动原生前端或模拟器，不能将离线通过描述为实机回归已完成。

## 训练场红豆修复与本轮交付（2026-09-07）

用户重新打开 MuMu 后，截图 SDK 正常取得训练场画面；此前主动关闭导致的无源状态不作为模拟器故障。用户随后明确实战由自己测试，本轮不再继续在线采集或操作模拟器。五代目水影因用户没有该忍者，暂缓自然截图验证，保留已有精确版本限定。

- 发现真实缺陷：六尾鸣人的名字已识别，但红豆的黄色/白色动态高光被当成未知，红色血条与短光晕还会误触发遮挡保护。先前单张修复在后续十帧里仍有八帧失败，未安装该中间版本。
- 最终修复限定于已识别的六尾红色配色：原校准豆体下部必须有足够红色证据；短光晕例外还需远处已衰减、连续两行豆体均明显亮于左右间隙。不移动采样中心，不把其他忍者的白光算豆，不放宽紫色/普通蓝豆规则。
- 新增原始十帧动态高光回归、800～2560宽缩放回归，以及空豆白光、全白、整片红光、水平红带、缺少忍者身份的负例；原九张特殊忍者回归仍通过。十帧最终均为左4右2，无未知；这些有限素材不是全场景准确率。
- 训练场没有账号文字时不猜我方边，浮窗提示「训练场·请选我方边」。选择我方左则显示对面右，手动优先；相关软件界面测试通过。当前原生窗口截图接口不兼容，未据软件测试宣称已用鼠标实点左右选项。
- 最新60秒只读观察3165次，均为训练场、左4右2、无采集错误。采集/分析/交付P95约20.51ms；存在并行测试负载，未作为受控性能提升对照，也不包含事件确认及显示器呈现。观察中没有真实替身事件，不能据此宣称实战开钟已验证。
- 普通忍者仍统一15秒，不需要先准确识别普通忍者名字才检测豆数。当前佐助名字OCR存在拒识；不以模糊错字放宽身份校验。通用姓名与特殊HUD的识别覆盖仍不完整。

本轮 `go test ./... -count=1 -timeout=180s`、`go vet ./...`、UI包race、红豆相关目标用例race及五个程序构建全部返回0。红豆race仅指选定回归用例，不是全项目race。证据与安装散列见 `F:\计时器\debug\live-features-20260907-111246`。安装保留旧版二进制备份，用户配置不改动。下一步等待用户自己的实战测试反馈，完整长期目标仍未完成。

## 用户反馈特殊忍者跳动、对面鸣人漏计后的修复（2026-09-07）

证据目录：`F:\计时器\debug\special-stability-20260907-114112`。用户实测反馈优先于之前的静态/有限回归，本轮没有将此前通过解释成实战问题不存在。

- 已复现：特殊名字被遮住一帧后，即便下一帧文字恢复，旧逻辑仍等待约500ms才搜索；这超过150ms观测连续性上限，可能重建豆数基线而漏计。新逻辑保留原模板位置作为重试提示，恢复帧重新验证当前原始像素；遮挡帧仍是未知，不冒充名字/豆数。
- 已复现：六槽名字短暂丢失会退回四槽，重新认到又切回六槽，两次重置事件基线。现短缺口仅保留六个未知占位，不用旧名字/配色或缓存豆数投票；提示限时1秒并受几何/时间域约束。四槽普通路径不改判色阈值。
- 取得双方均为六尾鸣人的自然训练场原图，视觉上左4右1。旧模板在1600宽能识别这张，但缩至800/960/1280宽时仅右侧失败，原像素全搜索最高约0.765，故不是粗搜索漏峰。新增由该图真实右侧姓名裁取的模板，原0.80阈值与0.08不同名字差距保持不变，800～2560宽回归通过。
- 新增右侧鸣人2→遮住名字→1→1、六槽名字遮挡后4→3的合成全链路事件测试，验证仅记一次、时间取第一张当前有效掉豆帧。这是注明合成的逻辑验证，不是实际替身动画或屏幕呈现延迟测量。
- 本轮只读采样中有25张已保存自然图，旧版回放也全部读为左4右1，未捕获用户报告的失败瞬间，不据此声称所有跳动/实战漏检已消除。另一个误漏 `-record` 的探针仅保存首图，其29条无图回放错误不计入识别错误率。

没有操作游戏对局。五代目水影继续暂缓。最终测试退出码、安装散列和未验证范围以本轮证据目录记录为准；继续等待用户实战反馈，完整目标尚未完成。

## 普攻按钮换皮肤引发场景误暂停（2026-09-07）

用户继续提供双方六尾HUD、左右同时问号的浮窗、六尾手掌和两种紫色普攻图案，并建议使用不随忍者变化的替身按钮。证据目录：`F:\计时器\debug\bar-effects-20260907-115605`。

- 新HUD裁图按局部坐标与15px槽距测试能读出左4右2；没有据此认定红条/颜色是根因，也没有继续放宽RGB阈值。
- 已复现场景门闩缺陷：旧备用证据只有普通普攻按钮。真实六尾画面对此模板得分约0.088，低于0.85；主场景路标被合成遮住后，原始豆数仍全部正确，结果却是 `Fighting=false, Hold=true`。UI因此清空当前豆显示，同时停止事件确认，符合左右同时问号而姓名仍在的现象。
- 按用户建议新增带mask的替身按钮备用证据，排除外部背景、底部豆装饰及中央数字区域；没有为紫色/六尾等每种普攻皮肤单独加模板。两个控件是替代关系，不要求普攻匹配才能用替身辅助。仍需同帧双侧豆区、已有对局上下文和同一布局；不能仅凭替身按钮开始新对局或把我方按钮当成对面替身事件。
- 800～2560宽、普攻区域完全移除、替身按钮合成变暗/中央遮挡均通过；替身也移除时拒绝继续，普通页面以及显式场景/几何/超时边界保持通过。
- 使用同一份已安装的 `F:\计时器\bin\timer-lab.exe` 做旧/新资源对照：同一个注明合成的右侧2→1→1序列，旧规则0事件，新规则恰好1事件，首个有效掉豆20ms、确认40ms、15秒冷却。此20ms是构造帧时间间隔，不是实战端到端延迟。没有在本轮操作或录制用户游戏。

此修复是资源更新，不改用户配置、不替换可执行文件、不用界面缓存掩盖问号。当前程序在启动时加载模板，用户须重新打开计时器才能载入新规则，MuMu无须关闭。全包测试、静态检查和本次相关race退出码记录在本轮目录；自然移动/技能特效下的最终效果仍需用户实测，未宣称所有遮挡或识别问题消失。

## 静止也抖：金豆闪光与界面状态混用（2026-09-07）

证据目录：`F:\计时器\debug\scene-state-20260907-121139`。用户允许只读观察并说明不移动也抖；没有操作游戏。

- 首次8秒394帧全部为 `fight/camp/hold=false`，其中13帧左豆未知、右豆始终2。不能把浮窗“待识别”理解成检测器真的频繁切换训练场/决斗场。第二次显式限量保存6张原图，确认左侧为普通佩恩，右侧为六尾鸣人：左侧特殊名字为空是预期，并非忍者名字丢失。此前口头的名字因果推断已纠正。
- 原图回归精确复现：左边第3颗金豆中央动态闪光变白，核心分类置信度落至0.24～0.52，低于原阈值，整排按合法序列检查变未知；右侧红豆仍正确。没有因此改动场景门闩或扩大名字缓存。
- 新增固定豆体上下部金色验证、同一横行左右间隙对比、远端衰减检查。两部位均需当前金色证据，至少一部位有足够轮廓区分，才容许中央高光参与判色。保持原校准中心、正常置信度阈值和事件确认规则；整片光、白色遮挡、只剩一侧颜色不能靠新例外投票。
- UI的 `hold` 仅反映场景/采集不确定，不再因任一侧豆未知而把整个“对局”改成“待识别”。真正未知仍显示该侧问号；每侧独立中断候选，另一侧有效掉豆仍能开钟；已确认计时照常流逝。不是缓存旧豆数消除视觉波动。
- 六张真实原图800～2560宽回归通过；加入光带/白色/单侧颜色负例、重复回放不误开钟、交替单侧未知不改变场景模式也不阻止对侧事件的测试。新增软件渲染单侧问号预览，未宣称实点原生窗口。
- 新版再只读观察8秒394帧，全部训练场、左4右2、无未知/采集错误。仅代表本段静止动画，不代表所有技能特效、实战替身准确率或显示延迟已验证。采集/分析/交付P95约22.46ms，不含源渲染、事件确认与浮窗呈现，未用作受控性能提升对照。
- 诊断CLI新增显式 `observe -save-uncertain N`，默认不保存像素；最多8张且合计64MiB，采样时深复制、结束后编码PNG，并在报告披露内存复制开销。新增默认禁用、数量/内存界限、错误图像和非零原点缓冲区复用测试。普通运行无自动截图归档。

全包测试、vet、相关race与五个程序的构建/安装状态以本目录验证JSON和退出码为准。用户配置保持不改；五代目水影自然验证继续暂缓。交付后等待用户自己的实战测试，长期目标未完成。

## 冷却边界漏计反馈与奥义动画退出保护（2026-09-07）

证据目录：`F:\计时器\debug\battle-boundaries-20260907-163428`。未操作或采集用户游戏；两张桌面截图已保存为报告附件，不能当成连续原始掉豆帧或奥义动画证据。

- 确定性复现UI与离线回放的退出计数缺陷：result命中与未知/VS帧交替，旧实现会跨动画累计10次后退出。现在未知、错误、换人等中断退出候选，不重置已确认冷却；重复/倒序采集时间不参与退出确认。新时间戳下相同静态结算像素仍可确认退出，不能把豆事件的“不同像素”规则错误套到静态菜单上。
- 模板清单新增minimumRegions，结算result要求两处独立不重叠区域同时达到原阈值。单一相似标记或同位置重复模板只能保持不确定，不能建立结算证据。既有真实胜利/失败页640～2560宽测试通过。尚无这次奥义终结动画原图，不能宣称已定位实际误中哪条模板。
- 决斗场已建立对局时，长时间未知/空白画面显示“决斗场 · 画面遮挡”，仍明确显示豆问号，不显示成退出，也不提供新豆事件票。合成3秒动画测试验证已确认计时和次数保留；真正结算经连续确认仍重置。
- 秒替身：检查到SideClock并不限制旧冷却未结束时的新事件。新增真实采样节奏与150ms观测断档保护下，旧冷却结束前40ms、正好结束、结束后40ms的连续掉1豆测试，全部记录第二次并显示新的15秒倒计时。没有改成单帧恢复/猜测掉豆，没有认定这已修复用户漏计；仍需覆盖掉豆瞬间的连续录屏/原始采样才能区分识别丢帧、基线中断或其他原因。

全量测试及选定race、最后UI补测结果见退出码文件。新版五个exe已构建暂存，安装状态见verification.json。用户config.json未改；清单资源已更新，运行中的旧程序仍使用启动时加载的旧资源。长期目标保持未完成，实战仍由用户验证。

## 换人豆数继承：首帧误升基线（2026-09-07）

用户明确豆数由下一人物继承，不是换人重置。当前代码的已识别VS返回路径会直接SyncReady首张画面。确定性回归复现：原3豆 → VS → 出场一帧4豆 → 3 → 3，会错误新增替身、移动原冷却起点；原3豆 → VS → 2 → 2又会吞掉首次真实下降。这是可复现代码路径，不等于已经抓到用户那次自然换人动画。

修复限定于“已在对局 + 明确VS换人”的继承恢复：保留双方独立已确认豆数、已有冷却和次数；返回帧走正常候选验证，不再强行覆盖基准。上下闪亮/变暗单帧不能投出事件，真实连续减一不一律跳过，计时起点取首次有效返回掉豆帧，不倒推到动画内部。返回变化至少需两帧，即使配置单帧确认也不能让一帧出场特效开钟。

无已知对局的VS、选人/排队不授权继承。坐标/槽数变化和换人期间采集错误优先作观测失效；已有计时保留，但不能跨无效坐标比较。继承豁免只跨明确换人暂停后的第一次有效观测，后续仍执行150ms等观测断档保护。UI与离线回放同步更新；原“首帧无条件同步”测试按用户澄清替换为继承与两帧验证，保留重复帧、未知单侧、几何变化和真实结算测试。

新增长玩家名报告：原图 `inbox/regressions/duel-long-account-tayuya-20260907.png`。本机系统OCR可认左春野樱/白方小，右多由也仍误读。实际姓名整行均在动态扫描范围内；收紧到文字本身以及多档二值实验仍失败。因此没有把它伪称为位置修复，没有添加错字替换/放宽姓名版本门槛，OCR生产代码未改。ROI覆盖测试只证明未截断，不证明文字识别成功。实验脚本与原始输出保存在本轮debug目录，姓名覆盖问题仍未解决。

验证与构建退出码、暂存/安装状态见本轮verification.json。尚未取得这次自然换人连续帧；用户配置未修改，未操作游戏。长期完整识别目标仍未完成。

## 跨回合差值不再冒充替身事件（2026-09-08）

修正上节2026-09-07继承恢复方案中的错误假设：原3豆 → VS → 2 → 2，只证明返回后当前有2豆，并不能证明本回合观察到了一次替身。旧测试把这个不可见区间的差值当成真实事件，因此虽然通过当时测试，仍可能在下一人物开局新增次数、重启15秒。

现在保留原豆数，直到返回画面重新看到相同豆数，或两次独立有效观测确认不同的当前豆数。后一种情况仅校准基线，不新增替身；已确认次数、冷却起点保持，已过期冷却不会复活。原3 → VS → 单帧4 → 3仍不抬高基线；原3 → VS → 3 → 2 → 2仍确认本回合可见的真实下降。不是固定屏蔽若干秒。

新的核心、UI和离线回放回归测试在修改前均复现多算一次，修改后通过。旧测试中关于“跨隐藏间隔减一必为替身”的断言已更正，而不是把用户的豆数继承规则改成重置满豆。

范围限制：本次明确修复已有的VS/同局换人恢复路径，没有新增画面回合识别器；未得到用户这次自然换人连续录像，不能证明场景门闩每次都会识别该边界，也没有办法仅凭不连续的豆数区分隐藏期间的真实使用与误读。用户实战验收仍待进行。未修改用户配置、名字识别、颜色/槽数、正常连续观察的冷却触发时刻。

### 本轮补充：真实录像里的开场白光（2026-09-08）

用户提供 C:\Users\Administrator\Videos\2026-09-08 05-26-10.mp4 后，只读分析17:10～17:18（30fps，1280×720屏幕录制，游戏裁区[8,65,1007,627]）。17:16.000前浮窗第6次，17:16.200画面中已变第7次、14.9秒；前后都是3颗左豆。17:16.033与17:16.067的白光覆盖第4颗空豆，旧RGB区间把它判成可用豆，置信得分分别约1和0.923（这不是正确概率）。

校准采豆层+SideClock对这7张真实连续解码帧，修改前得到3→未知→4→4→未知→3→3，错误新增1次；修改后白光下第4颗为未知，不再形成假恢复，事件数0。另加注明为合成的真正3→2后续下降，仍按首次采集时刻确认1次。800～2560宽空豆负例、普通/红/紫/金豆原有回归通过。

修复增加普通蓝色核心与固定相邻间隙的空间比较：可用颜色还必须有可分辨的豆体，不接受空心暗豆被半透明白/蓝光照亮后仅凭RGB过线投票。不移动采样中心、不向更亮的邻豆搜索，不按某个忍者硬编码，不加开场定时禁用。遮挡仍如实未知，已有倒计时照常继续。

限制：这是压缩缩放后的30fps录屏，不是原MuMu SDK高频采集。完整240帧经过旧场景门闩的离线回放已经是0事件，因为其中一张误判豆的画面被该回放的门闩暂停；不能把它伪称成完整旧程序回放也复现1次。已确定复现的是采豆+计时层错误链条，并与录屏中实际浮窗的第6→7次时间对应；最终仍需用户用新版实战验证。

### 场景上下文与单豆不确定解耦（2026-09-08）

本轮不靠前端沿用旧豆掩盖问题。已有场景代码在750ms缺少完整依据后清空上下文，且两个竞争模板帧就可改采样布局；采豆的完整前缀检查又会把中间一颗未知扩散成整侧未知，导致场景延续所需的左右HUD证据一起消失。

修正如下：
- 场景上下文与采样许可分开。未知/空白只停止观测，不因超时忘记原场景；恢复须重新具备当前两侧至少各3个可信槽及对局控件。明确大厅、结算、VS等页面、尺寸变化或时间倒退仍清除上下文。单凭旧场景不能投票开钟。
- 竞争布局先与当前旧布局的有效HUD/控件核对。旧布局仍被当前画面支持时不切；真正竞争布局需持续300ms且有不同画面。这只确认坐标系切换，不延迟正常掉豆确认。
- 兼容亮豆前缀的部分观测仅保留本帧各槽已知状态，不填未知槽；已知暗豆后出现已知亮豆仍整侧拒绝。完整豆数和事件依然要求每个槽明确，不把部分槽相加冒充总数。
- 金豆白色闪光在999/1280等宽度时，旧固定外围统计被暗边占比拖垮。现在核对上下两个固定内部区域中连续的金色豆体行及两侧空间对比；具备真实豆体才接纳压缩后略偏色的白色中心，不移动采样中心、不从历史豆数猜测。

证据目录：debug/scene-bead-stability-20260908-065920。用户同一录像17:38～17:48（300帧，30fps）前后回放：未知侧帧4→0，确认事件均0，原始场景在此段前后均为fight/duel且无Hold。因此本段直接复现的是金豆闪光未知，不冒称它复现了所有场景跳动。12张真实邻接/闪光帧、800～2560宽回归通过；场景上下文/竞争布局故障以单元序列及真实HUD加明确标注的合成遮挡回归验证。保留原开场白蓝光负例，240帧回放事件仍为0；原25帧双六尾、10帧红豆、6帧训练闪光回归均无未知侧。

全包测试、detect/engine/factory/rgb/ui竞态测试及go vet通过。一个旧回放测试的“750ms后场景必须为空”断言更新为“保留场景但Fighting=false、Hold=true且不产生事件”，并非放宽计时条件。本轮未实机输入或采集；普通名字/特殊规则未修改。真正遮挡仍允许显示问号并停止该侧确认，不承诺所有特效都能读出。仍需用户实战验收，长期目标未完成。

### 自审发现的三个独立缺陷修复（2026-09-08）

证据目录：`F:\计时器\debug\review-fixes-20260908-094103`。对应上轮只读审查 `F:\计时器\debug\code-review-20260908-084553` 的三个失败用例，用户授权后将其纳入正式回归；没有以原全包通过代替故障验证。

1. **普通金色分支漏了豆体校验**：真实HUD叠加明确注明的合成平坦黄光，原先得到3→4→4→3→3并制造一次事件。现在普通饱和金色也核对固定中心与间隙的空间对比；白色闪光仍走独立上下豆体验证。修后平坦/空心黄光为未知，事件0；后接注明合成的真实可见3→2仍恰好确认一次，起点为首个有效掉豆帧。最初过窄纵向核验使真实第153帧在950宽漏读，已在固定豆体范围内修正，12张金豆原图800～2560宽再次通过，未放宽RGB分类阈值。
2. **场景短暂停顿清空已确认OCR证据**：只在已有同一fight上下文、坐标一致且当前字形证明仍成立时，最多保留2秒已确认字段；暂停期间不输出，不提交新OCR，不保留候选票。进入暂停增加代次，迟到工作不能跨暂停确认；恢复核验后立即使用未变字段。擦除文字、明确VS、几何/布局改变、关闭、时间回退、长暂停测试均要求失效；勘九郎原图与已有词框加合成背景变化验证不会因无关背景丢名。此项减少重新识别的等待，不声称首次OCR覆盖率因此提高，也不让换人沿用旧名字。
3. **布局切换把断档时间当成持续证据**：两个相隔5秒的竞争标签原先能直接切换。现在超过300ms观测间隔、显式缺帧或非递增时间戳会清除未确认布局候选，但保留已经建立的场景。断档后必须重新连续确认；原有当前HUD优先与持续竞争标签确实能够切换的正例保持通过。

验证：全量 `go test ./... -count=1`、engine/rgb/hudtext/ui的race、`go vet ./...` 均通过；原图经本机Windows OCR进入NewLive的集成测试通过（山中井野/白方小、正确右侧），未进行新游戏采集或输入。5个程序构建、配置散列、安装状态记录在本目录JSON。

已有录像/原图序列回放：金豆300帧未知侧0、双六尾25帧未知侧0、红豆10帧未知侧0、训练闪光6帧未知侧0；开场白蓝光240帧仍有72帧未知侧，确认事件0，与上一版结果一致，没有用旧豆消掉遮挡问号。5组均无读图错误。该回放不启用后台OCR，名字暂停另由上述独立测试验证；回放耗时不是实机端到端延迟，也没有给出全场景准确率。

用户配置、普通忍者字典、手动边优先、换人继承处理和五代目水影规则均未修改。尚需用户实战验收；长时间遮挡、首次OCR误读等未被这三个针对性修复全面解决，长期目标仍未完成。
